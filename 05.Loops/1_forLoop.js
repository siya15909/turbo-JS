//FOR LOOP
const n=prompt("Enter the value of n : ");
let sum=0;
for(let i = 0;i<n;i++){
    sum+=i;
}
alert(`The sum of first ${n} numbers is ${sum}`);
/**
 * Syntax =>
 * for(intialiization ; condition ; updation){
 * statements ;
 * }
 */