/**Syntax =>
 * for(let val of StrVar){
 *     do somework
 * }
 * No initialization , updation and condition needed 
 * Used for strings and arrays
 */
let string = 'JAVASCRIPT'
for (const iterator of string) {
    console.log(iterator);
}